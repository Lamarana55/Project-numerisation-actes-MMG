export const environment = {
  production: true,
  // URL relative : Nginx proxie /api/v1 → backend:8091
  apiURL: '/api/v1',
  refreshInterval: 600000, // 10 minutes en production
  appName: 'Dashboard Statistiques RAVEC',
  version: '1.0.0'
}
